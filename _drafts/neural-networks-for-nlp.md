# Algorithm 1

decision(x, w, b):
    if dot_product(w, x) + b > 0
        return true
    else
        return false

# Algorithm 2

w = 0
b = 0

while _not converged_ do
    d = 
    for _each training example_ xi in X do
        continue if 
    end
end
